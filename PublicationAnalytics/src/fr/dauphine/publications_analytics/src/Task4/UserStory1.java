package fr.dauphine.publications_analytics.src.Task4;

import java.util.List;
import java.util.Map;

import net.sf.saxon.tree.iter.ListIterator;

public class UserStory1 extends XMLPublicationTask4{
	
/*	public void get_author_coauthor(String file_name, String select, String sortedBy){
		List<String> listAuthor = get_data_by_type(file_name, select, sortedBy);
		Map<String,List<String>> test = get_cohauthor_by_data(file_name,listAuthor,sortedBy);
		
		for (String mapKey : test.keySet()) {
			// utilise ici hashMap.get(mapKey) pour accéder aux valeurs
			System.out.println("Key: "+mapKey);
			for(int i=0;i< test.get(mapKey).size();i++){
				System.out.println("Coauthors: "+ test.get(mapKey).get(i));
			}	
			 	System.out.println("--------------------------------***");
	}
	}*/
}
